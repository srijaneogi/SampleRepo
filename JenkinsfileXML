pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                script {
                  def props = readFile(file: "books.xml")
                  echo "$props"
                  def xml = new XmlSlurper().parseText(props).catalog.book.title
                  //echo "Printing xml"
                  //echo "$xml"
                  //def env = xml.catalog.book.title
                    
                  //echo "variable fetached= ${env}"
                  
                  def elementToFind = 'n'
def result = pxml.'**'.findAll{it.name() == elementToFind}
println result
println result[0].text() 
                }
            }
        }
    }
}
