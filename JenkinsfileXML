pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                script {
                  def props = readFile(file: "books.xml")
                  echo "$props"
                  def a = props['title']
                  echo "$a"
                }
            }
        }
    }
}
